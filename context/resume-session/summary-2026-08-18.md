# Resume-Session Summary — 2026-08-18 (jul-31 → qa-session-aug-17 shipped)

**Purpose**: single entry point so a **new clean context** can resume mi-empresa-app without re-deriving history.
**Supersedes**: `summary-2026-08-17.md` (keep for jul-31→aug-6 archaeology).
**Last verified**: staging QA **PASS** after `d-YRWXPRH7L` + Amplify **job 16**. Staging migrations **29**.

---

## 0) Bootstrap (do this first in a new session)

```bash
# 1) Read this file fully
# 2) Quick live check
curl -s https://miempresa-api-stg.disruptiveexp.com/api/v1/health
curl -s -o /dev/null -w "%{http_code}\n" https://miempresa-stg.disruptiveexp.com
# 3) QA logins (custom domain ONLY — not *.amplifyapp.com)
./backend/prisma/test-db/get-qa-creds.sh --stage staging --profile disruptive --region us-east-1
# 4) Local stack: backend :3101 · frontend :3100 · postgres docker :15432
#    admin|empleado|auditor|operador@miempresa.com / <redacted>
#    + profesor@miempresa.com / auxiliar@miempresa.com / <redacted> (local seed only)
```

**Hard rules (never skip)**
- Commit **only** on explicit user instruction. Developer always commits himself.
- **No prod** AWS. CodeDeploy group `miempresa-prod` EXISTS — never target it (`miempresa-staging` only).
- Always AWS `--profile disruptive --region us-east-1`.
- Never `prisma migrate diff --shadow-database-url`.
- Never generic `pkill node/tsx` (local prod bun may sit on **:4142**). Restart local BE via `lsof -ti :3101`.
- Orchestrator does **not** implement product code; workers do. After a developer **stop**, do not INTERVENTION-resume without a new yes.
- **Workers never send `plan_approval_response`** (lead-only). Plain `PLAN-APPROVAL:` text + WAIT.
- Migrations gitignored → ship inside CodeDeploy zip; AfterInstall runs `prisma migrate deploy`.
- Prefer existing tooling: `ssh-to-instance.sh`, PEM `~/.ssh/miempresa-lightsail-key.pem`. Lightsail not SSM EC2.
- Working-tree CodeDeploy ships **everything dirty**. Call out ride-alongs at R0.

---

## 1) State at a glance

| Layer | State |
|---|---|
| **Git** | Branch `main`, HEAD **`8e07055`** (partial). **Still huge dirty tree** (~136+ paths including this cycle). |
| **Local DB** | **29** migrations (newest `20260818113726_add_nomina_bonos_and_registro_actividades`). |
| **Staging** | **Current through qa-aug-17 + users-tipo fix.** BE **`d-YRWXPRH7L`**, Amplify **job 16**, 29 mig. |
| **Prod** | Does not exist. |

### Staging endpoints
| What | URL |
|---|---|
| FE | https://miempresa-stg.disruptiveexp.com |
| API | https://miempresa-api-stg.disruptiveexp.com/api/v1 |
| Login | https://miempresa-stg.disruptiveexp.com/login (**custom domain only**) |

### Staging QA profiles
| Profile | Role | Notes |
|---|---|---|
| `qa-admin@miempresa.com` | ADMIN | lock/unlock; user create |
| `qa-gerontologa@miempresa.com` | EMPLEADO+GERONTOLOGA | actividades **read-only** |
| `qa-contratos@miempresa.com` | EMPLEADO+CONTRATOS | GET cargos; registrar nómina unlocked; actividades **read-only** |

No **permanent** staging users for PROFESORES/AUXILIARES. Create ephemeral via `POST /users` (`tipoEmpleado` now accepts all 4 tipos) + linked `empleadoId`.

### AWS constants
Profile `disruptive` / `us-east-1` · Lightsail `miempresa-backend-staging` @ `54.144.25.72` · CodeDeploy `miempresa-app` / **`miempresa-staging` only** · Amplify `d1nsxjyualdzdu`/staging · backups `miempresa-backups-540657241795-staging`.

---

## 2) Deployed stack history (staging)

| When | Release | BE deploy | Amplify | Notes |
|---|---|---|---|---|
| jul-31 | qa-jul-24 | `d-OJON0UVVK` | job 12 | mig 24 |
| aug-04 | jul-31 + lock + TINETTI | `d-EDEN0GJYK` | job 13 | mig 25–26 |
| aug-06 | fixes-aug-6 | `d-MR1QVXIZK` | job 14 | mig 27–28 |
| aug-14 | qa-aug-6 followup | `d-13RWC0Q4L` | job 15 | CONTRATOS contratos; SIGNOS/BOLETIN v2 |
| **aug-18** | **qa-session-aug-17** | **`d-FD0BEVH7L`** then **`d-YRWXPRH7L`** | **job 16** | mig 29; bonos; actividades; users tipo fix |

**Latest staging truth = `d-YRWXPRH7L` / Amplify job 16 / 29 mig.**

Backups: `pre-jul24-qa.sql.gz` · `pre-jul31-aug04.sql.gz` · `pre-aug6.sql.gz` · `pre-aug6-followup.sql.gz` · **`pre-aug17.sql.gz`**.

---

## 3) Cycle qa-session-aug-17 (what shipped)

Source: `context/user-feedback/qa-feedbacl-aug-17.md` (cleaned). **Pendo `initial-ask.md` was a wrong path — discarded.**

Contract: `development/qa-session-aug-17/orchestration-ctx/decisions/contract-schema-qa-aug-17.md`

| # | Item | Key impl |
|---|---|---|
| R1 | Empleados **Activos \| Inactivos** tabs, default Activos, **no Todos** | `frontend/app/pages/empleados/index.vue` `estadoFilter='ACTIVO'` · testids `empleados-tab-activos/inactivos` |
| R2 | CONTRATOS GET cargos 403 | **Route exception** on `GET /empresa/cargos` only. `CONTRATOS.empresa` stays **false**. Writes ADMIN. |
| R3 | Nómina **Bonos**; Total = mensual + bonos (**aportes not added**) | `NominaPeriodo.bonos` Decimal(12,2) NULL. `BONOS_ALLOWED` = FIJO\|INDEF. FE field order: mensual → bonos → subtotal → aportes (referente) → total. |
| R4 | Periodos 400 + CONTRATOS write | FIJO already did not require jornada (live 400 was **not** jornada). Real defect was formula `V+aportes`. POST/PUT `/nomina/periodos` = `requireEmployeeUnlocked` (not ADMIN). OPS/OBRA still require `CUENTA_COBRO` file. |
| R5 | SIGNOS/BOLETIN no patient fields | **Already shipped** v2. Re-confirmed on staging. |
| R6 | **Registro de actividades** under Asistencia | Domain `actividades`. Table `registro_actividades` unique `(empleadoId, fecha)`. ACL: ADMIN full; PROFESORES/AUXILIARES own+today; GERONTO/CONTRATOS read-all. |

**R6 followup (QA found)**: `users.routes.ts` Zod still `tipoEmpleado: GERONTOLOGA` only + PATCH rejected others. Fixed + redeployed `d-YRWXPRH7L`.

### Formulas (locked)
- FIJO/INDEF: `subtotal = totalPagado = valorMensual + bonos` (aportes stored, not added)
- OBRA: `total = valorMensual`; no bonos; cuenta cobro required
- OPS: **unchanged** this cycle (still cuenta cobro + jornada)

### Error codes
`DOMAIN_FORBIDDEN` · `EMPLOYEE_LOCKED` · `EMPLEADO_REQUIRED` · `DUPLICATE_DAY` · field `bonos` · `archivos.CUENTA_COBRO`

---

## 4) Authoritative contracts

| Topic | Path |
|---|---|
| **aug-17** (tabs, cargos GET, bonos, periodos, actividades) | `development/qa-session-aug-17/orchestration-ctx/decisions/contract-schema-qa-aug-17.md` |
| jul-31 Nequi / valorMensual / EPS | `development/qa-session-jul-31/orchestration-ctx/decisions/contract-schema-qa-jul-31.md` |
| aug-6 roles / instruments | `development/fixes-features-aug-6/orchestration-ctx/decisions/contract-fixes-features-aug-6.md` |

Live matrix: `backend/src/middleware/domainAccess.ts` + `frontend/app/composables/useDomainAccess.ts` (now **12 domains** incl. `actividades`).

---

## 5) Key code patterns (this cycle)

| Pattern | Where | How to extend |
|---|---|---|
| Today Bogotá | `backend/src/utils/dateBogota.ts` `serverTodayBogota()` | **Import the binding** in asistencia.routes — `export { x } from` is **not** a local name (`tsc` failed). Re-export kept for old imports. |
| Cargos GET exception | `empresa.routes.ts` — domain applied **per-route**, GET cargos allows CONTRATOS | Do **not** flip `empresa=true` |
| Periodos unlock | `requireEmployeeUnlocked` on body `empleadoId` | DELETE periodos stays ADMIN |
| Actividades ACL | `actividadService` own-filter + today; matrix create-only / read-only | Unique per day → 409 `DUPLICATE_DAY` |
| User tipos | `users.routes.ts` Zod 4-value enum | Keep in sync with `TipoEmpleado` |
| Sidebar | Drawer (`AppSidebar` `visible` from hamburger) | Playwright must open it; labels live in `<aside a>` |
| FE tabs | default `ACTIVO`; no empty/Todos | Stats cards must not reset filter |
| v-model | refs on actividades form | not `const reactive()` |

---

## 6) Migrations (local = staging = 29)

| # | Dir | What |
|---|---|---|
| 24–28 | (prior) | jul-24 … PROFESORES/AUXILIARES + centro-costos |
| **29** | `20260818113726_add_nomina_bonos_and_registro_actividades` | `nomina_periodos.bonos` NULL; `registro_actividades` |

Gitignored — only travel via CodeDeploy zip.

---

## 7) Tests created / updated

### Backend (Playwright)
| File | What |
|---|---|
| `backend/tests/empresa/contratos-get-cargos.spec.ts` | R2 6 tests |
| `backend/tests/nomina/nomina-bonos-total.spec.ts` | R3/R4 6 tests |
| `backend/tests/actividades/registro-actividades-acl.spec.ts` | R6 7 tests |
| `backend/tests/users/tipoempleado-matrix.spec.ts` | POST /users PROFESORES+AUXILIARES |
| `backend/tests/rbac/matrix-parity.spec.ts` | + `actividades` cells (12 domains) |
| `backend/tests/staging/qa-aug17-staging.spec.ts` | Live staging 8 tests |

Independent local: **23/23** cycle + matrix. Staging API: **8/8**.

### Frontend
| File | What |
|---|---|
| `frontend/tests/local-qa/aug17-qa-frontend.spec.ts` | mocked 11 tests (R1/R3/R6) |
| `frontend/tests/staging/qa-aug17-staging-ui.spec.ts` | live 3 tests (tabs, nav, cargos) |

---

## 8) Frontend / backend files (this cycle)

**BE**: `schema.prisma` · mig 29 · `seed.ts` (profesor/auxiliar + empleadoId) · `domainAccess.ts` · `empresa.routes.ts` · `nomina.routes.ts` · `nominaService.ts` · `actividades.routes.ts` · `actividadService.ts` · `routes/index.ts` · `utils/dateBogota.ts` · `asistencia.routes.ts` (import) · `users.routes.ts`

**FE**: `empleados/index.vue` · `nomina/index.vue` · `actividades/index.vue` · `app.config.ts` (item after Asistencia, `pi pi-list`) · `useDomainAccess.ts` · `shared/types/api.ts`

---

## 9) Orchestration lessons (aug-17)

- Wrong input path (`pendo-design-tool/initial-ask.md`) — confirm source before intake.
- **Worker must never self-approve** (`plan_approval_response`). Lead writes `decisions/*-approval.md` + `PROCEED:`.
- After developer **stop**, orchestrator must **ask** before INTERVENTION-resume.
- Do not `shutdown_request` to “freeze” — send `WAITING:`. Shutdown is user/8h only.
- Task namespace reset on new session → rebuild TaskList from `team-status` (Phase 0 step 2b).
- `export { fn } from` ≠ usable `fn()` in same file.
- Working-tree deploy + ride-alongs (centro-costos already on staging).
- R4 reported 400 ≠ jornada; diagnose live body first (here: formula + later CUENTA_COBRO on OPS/OBRA).

---

## 10) Open / next

1. **Uncommitted tree** still huge (HEAD `8e07055` slice only).
2. Optional: extend `seed-qa.ts` + SSM for permanent `qa-profesor` / `qa-auxiliar`.
3. Centro-costos still not independently QA’d.
4. Pre-existing: certificates 500 filter; print CSS; patient-notes TZ.

---

## 11) Grep hooks

```
resume-session aug-18 d-YRWXPRH7L d-FD0BEVH7L amplify-job-16 29-migrations
bonos registro_actividades requireEmployeeUnlocked GET-cargos-exception
serverTodayBogota dateBogota EMPLEADO_REQUIRED DUPLICATE_DAY
empleados-tab-activos nomina-bonos actividades-texto
tipoEmpleado MATRIX_TIPOS users.routes
pre-aug17.sql.gz qa-session-aug-17
```

---

## 12) Doc index

| Kind | Path |
|---|---|
| **This resume** | `context/resume-session/summary-2026-08-18.md` |
| Prior | `context/resume-session/summary-2026-08-17.md` |
| Cleaned stories | `context/user-feedback/qa-session-aug-17-cleaned.md` |
| Contract | `development/qa-session-aug-17/orchestration-ctx/decisions/contract-schema-qa-aug-17.md` |
| Handoff | `development/qa-session-aug-17/06-handoff.md` |
| QA plan / report | `development/qa-session-aug-17/qa-validation-plan.md` · `qa-validation-report.md` |
| Runbook | `context/implementation-plan/staging-release-qa-session-aug-17-runbook.md` |
