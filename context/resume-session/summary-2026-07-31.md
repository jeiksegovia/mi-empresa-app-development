# Resume-Session Summary — 2026-07-31 (qa-session-jul-24 shipped to staging)

**Purpose**: single entry point so a **new clean context** can resume mi-empresa-app without re-deriving history.
**Supersedes**: `summary-2026-07-22.md` (keep for jul-4→22 archaeology).
**Last verified**: staging health **200** (API + FE) after release `staging-release-qa-jul24` (CodeDeploy `d-OJON0UVVK`, Amplify job 12).

---

## 0) Bootstrap (do this first in a new session)

```bash
# 1) Read this file fully
# 2) Quick live check
curl -s https://miempresa-api-stg.disruptiveexp.com/api/v1/health
curl -s -o /dev/null -w "%{http_code}\n" https://miempresa-stg.disruptiveexp.com
# 3) QA logins (custom domain ONLY — not *.amplifyapp.com)
./backend/prisma/test-db/get-qa-creds.sh --stage staging --profile disruptive --region us-east-1
# 4) Local stack (if coding): backend :3101 · frontend :3100 · postgres docker :15432
#    admin|empleado|auditor|operador@miempresa.com / <redacted>
```

**Hard rules (never skip)**
- Commit **only** on explicit user instruction (tree is huge and multi-cycle; qa-jul24 is UNCOMMITTED).
- **No prod** AWS. CodeDeploy group `miempresa-prod` EXISTS — never target it (use `miempresa-staging`).
- Always AWS `--profile disruptive --region us-east-1`.
- Never `prisma migrate diff --shadow-database-url`.
- Never generic `pkill node/tsx` (local prod bun may sit on **:4142**).
- Orchestrator (planify-team) does **not** implement product code; workers do.
- **Migrations are gitignored** → they ship inside the CodeDeploy artifact (packages working tree), applied on-instance via `npx prisma migrate deploy`.

---

## 1) State at a glance

| Layer | State |
|---|---|
| **Git** | Branch `main`, HEAD **`d2e21ad`** ("changed implemented jull-22 work session" — user committed jul-22 backlog). **qa-jul24 work is UNCOMMITTED** working tree. |
| **Local DB** | **24** migrations (newest `20260731203612_qa_jul24_cargos_efectivo_valormensual`). |
| **Staging** | **Current through qa-session-jul-24.** BE **`d-OJON0UVVK`**, Amplify **job 12**, 24 mig. Prior: `d-WSU9IQ3QK`/job 11 (jul-22). |
| **Prod** | Does not exist. |

### Staging endpoints
| What | URL |
|---|---|
| FE | https://miempresa-stg.disruptiveexp.com |
| API | https://miempresa-api-stg.disruptiveexp.com/api/v1 |
| Login | https://miempresa-stg.disruptiveexp.com/login (**custom domain only**) |

### Staging QA profiles (passwords via SSM / `get-qa-creds.sh`)
| Profile | Role | Scope |
|---|---|---|
| `qa-admin@miempresa.com` | ADMIN | full |
| `qa-gerontologa@miempresa.com` | EMPLEADO + GERONTOLOGA | pacientes/fichas/instrumentos; no asistencia/nómina |
| `qa-contratos@miempresa.com` | EMPLEADO + CONTRATOS | empleados/nómina/asistencia/certificados; pacientes create-only; no instruments; **asistencia PUT = today only** |

### AWS constants
| Item | Value |
|---|---|
| Profile / region | `disruptive` / `us-east-1` |
| Instance | `miempresa-backend-staging` @ `54.144.25.72` (ec2-user, /opt/miempresa/app) |
| CodeDeploy | app `miempresa-app` · group **`miempresa-staging` only** |
| Amplify | `d1nsxjyualdzdu` branch `staging` |
| Buckets | backups `miempresa-backups-540657241795-staging` · BE `miempresa-artifacts-…-staging` · FE `miempresa-frontend-artifacts-…-staging` |

---

## 2) Deployed stack history (staging)

| When | Release | BE deploy | Amplify | Notes |
|---|---|---|---|---|
| jul-20 | nomina-asistencia | `d-8OHAZOPOK` | job 10 | migration 23 |
| jul-22 | fixes-jul-22 | `d-WSU9IQ3QK` | job 11 | no new mig; instruments v2 + FE fixes |
| **jul-31** | **qa-session-jul-24** | **`d-OJON0UVVK`** | **job 12** | migration 24; cargos delete+recreate, EFECTIVO, valor_mensual |

**Latest staging truth = row jul-31.**

---

## 3) qa-session-jul-24 cycle (just shipped) — 7 items

Source raw: `context/user-feedback/qa-session-jul-24-raw.md` · cleaned: `…-cleaned.md`.
Contract (SSOT): `development/qa-session-jul-24/orchestration-ctx/decisions/contract-schema-qa-jul-24.md`.
Implemented notes: `context/plan-implemented/qa-session-jul-24-implemented.md`. Handoff: `development/qa-session-jul-24/06-handoff.md`.

| # | Item | Key impl |
|---|---|---|
| R1 | EFECTIVO + Nequi "llave" | `MedioPagoNomina += EFECTIVO`; llave regex email\|alnum(6-25). `employees.routes.ts`, `empleados/nuevo.vue`+`[id]/editar.vue` |
| R2 | Partial payment edit + RBAC | `employeeService.updateEmployee` merge-validation; ADMIN+CONTRATOS |
| R3 | Remove Cargos block (UI only) | Info Laboral card removed in nuevo/editar; backend `Cargo` model kept |
| R4 | Default cargos (10) | `DEFAULT_CARGOS` (empresaService.ts); migration delete+recreate; **all contratos → `Temporal`** (decision D1) |
| R5 | Pago preview | `empleados/[id]/index.vue` `medioPagoPreview` card |
| R6 | Asistencia RBAC | `serverTodayBogota()` (Intl en-CA America/Bogota); CONTRATOS today-only→403, ADMIN any; `notas` field |
| R7 | Contract salary by tipo | `Contrato.valorMensual`; OPS→valorJornada, others→valorMensual; `nominaService.resolveCalcFields` branches |

**Decisions**: D1 cargos = full delete+recreate + reassign ALL contratos → Temporal; D2 valorMensual on Contrato only (NominaPeriodo has no column, passed as calc override); D3 Cargo model UI-removal only; D4 staged.
**Tests**: BE 3 new specs + 7 updated (67 pass; 3 pre-existing port-3001 = TEST-ENV). FE `frontend/tests/local-qa/jul24-qa-frontend-*.spec.ts`.
**Staging release**: runbook `context/implementation-plan/staging-release-qa-jul24-runbook.md` (R0–R5, backup `pre-jul24-qa.sql.gz`). Smoke: all 7 + RBAC + 9-page FE PASS.

### ⚠️ Staging smoke side effects (revert via R1 backup if clean slate wanted)
Employee 1 (JAEKOV): contratos id1(OPS)/id2(TERMINO_FIJO) now `cargo=Temporal`; contrato id2 `valorMensual=1300000`; nomina periodo id1 (2026-07); asistencia 2026-07-30(ADMIN)/07-31(CONTRATOS).

---

## 4) Authoritative contracts (SSOT — do not invent names)

| Topic | Path |
|---|---|
| **qa-jul-24** (payment/cargos/asistencia-rbac/salary) | `development/qa-session-jul-24/orchestration-ctx/decisions/contract-schema-qa-jul-24.md` |
| Instruments base | `development/instrumentos-dynamic-fichas/orchestration-ctx/decisions/schema-contract-instrumentos-dynamic-fichas.md` |
| RBAC (TipoEmpleado GERONTOLOGA\|CONTRATOS) | `development/fixes-jul17-2/orchestration-ctx/decisions/contract-fixes-jul17-2.md` |
| Nómina/asistencia base | `development/nomina-asistencia-jul-18/orchestration-ctx/decisions/schema-contract-nomina-asistencia-jul-18.md` |
| Jul-22 fixes | `development/fixes-jul-22/orchestration-ctx/decisions/schema-contract-fixes-jul-22.md` |

---

## 5) Repo structure (fast map)

```
mi-empresa-app-development/
├── backend/
│   ├── prisma/
│   │   ├── schema.prisma            # models + enums (MedioPagoNomina, TipoContrato, TipoCuentaBanco…)
│   │   ├── migrations/              # GITIGNORED; 24 dirs; ship via artifact
│   │   ├── seed.ts
│   │   └── test-db/                 # seed-qa-staging.sh, get-qa-creds.sh, db-tunnel via infra
│   ├── src/
│   │   ├── routes/                  # employees, asistencia, nomina, empresa, patients, instruments, certificates
│   │   ├── services/                # employeeService, asistenciaService, nominaService, empresaService(DEFAULT_CARGOS), cargoEmpresaService, patientService…
│   │   ├── middleware/              # domainAccess.ts (DOMAIN_ACCESS matrix), auth, validate
│   │   ├── generated/prisma/        # generated client
│   │   └── config|constants|controllers|types|utils
│   ├── tests/                       # {employees,asistencia,nomina,instruments-dynamic,patients,rbac}/*.spec.ts (Playwright)
│   └── infrastructure/db/scripts/   # reset-staging-db.sh, db-tunnel
├── frontend/
│   ├── app/
│   │   ├── pages/                   # empleados/{index,nuevo,[id]/{index,editar,historial}}, asistencia, nomina, pacientes, instrumentos
│   │   ├── components/instrument/   # DynamicGroupInfoField.vue, InstrumentResultView.vue, types.ts
│   │   ├── composables/             # useDomainAccess.ts, useUnsavedGuard.ts
│   │   ├── stores/                  # auth store (rol, tipoEmpleado)
│   │   └── layouts|middleware|plugins|utils
│   ├── tests/                       # local-qa/, fixes-jul-22/, asistencia/, empleados/, nomina/ (Playwright)
│   └── infrastructure/scripts/deploy-frontend.sh
├── context/
│   ├── user-feedback/               # qa-session-*-raw/cleaned.md
│   ├── resume-session/              # THIS file (newest wins)
│   ├── implementation-plan/         # staging-release-*-runbook.md, *-checklist.md
│   └── plan-implemented/            # {slug}-implemented.md (grep-optimized summaries)
├── development/{slug}/              # planify-team artifacts: 00-intake, {slug}-plan, 06-handoff,
│   │                               #   orchestration-ctx/{team-plan,team-status,decisions/}, tasks/W*/
│   └── orchestration-learnings/     # 00-index + patterns/briefing/lifecycle
├── .claude/                         # skills/planify-team, agents/pt-*, hooks, settings.json
├── plans/ · research/ · scripts/ · tmp/
```

---

## 6) Verification commands

```bash
# Local BE qa-jul24
cd backend && npx playwright test \
  tests/employees/employees-partial-medio-pago.spec.ts \
  tests/asistencia/asistencia-rbac.spec.ts \
  tests/nomina/contrato-valormensual.spec.ts --reporter=list
# Local FE qa-jul24
cd frontend && npx playwright test tests/local-qa/jul24-qa-frontend --reporter=list
# Prior cycles: see summary-2026-07-22.md §6
```
FE e2e often needs `TEST_FRONTEND_URL` / `TEST_API_URL` host match (Tailscale IP, not bare localhost cookies).

---

## 7) Staging release playbook (next time)

Pattern: planify-team `pt-devops-infra` worker + `.claude/skills/planify-team/release-protocol.md` (R0–R5).
1. R0 preflight (read-only, **row counts on any table a migration touches**) → CHECKPOINT.
2. R1 backup (dump → bucket, verify 2 paths, restore cmd in runbook) → R2 skip/reset → R3 seed-qa → R4 CodeDeploy `miempresa-staging` + on-instance `migrate deploy` (**point of no return**) → R5 Amplify + post-deploy smoke.
3. Auto-chain only when user says "auto after backup" / "end-to-end"; else PROCEED gates.
4. Latest example: `staging-release-qa-jul24-runbook.md`.

---

## 8) Open / next (pick one)
1. **Git commit** qa-jul24 working tree (user must ask).
2. **Revert staging smoke side effects** from R1 backup (JAEKOV contratos/nomina/asistencia) — optional.
3. New QA / features from next raw feedback under `context/user-feedback/`.
4. Reclassify the 2 staging contratos off `Temporal` if real cargo semantics needed.
5. Pre-existing debt carried from jul-22 (certificates list filter 500; print CSS).

---

## 9) Orchestration lessons (carry forward)
- Contract-first multi-worker: schema → backend ∥ frontend → QA; QA tests the contract, not implementation.
- Gate irreversible phases; R0 row-counts before destructive migration (qa-jul24 caught local QA-fixture: 1 cargo + all contratos on it).
- Env pty pressure real: system hit **87 panes / ~85 claude procs** → `fork failed`. Shut down parked workers; respawn under fresh name (collision guard). Don't blanket-pkill.
- Structured `shutdown_request`; mark SHUTDOWN only on `teammate_terminated`.
- Agent Teams env: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`; spawn from **repo root**.

---

## 10) Grep hooks
```
resume-session jul-31 d2e21ad 24-migrations qa-session-jul-24
d-OJON0UVVK amplify-job-12  d-WSU9IQ3QK amplify-job-11
MedioPagoNomina EFECTIVO medioPagoNequi llave valorMensual valor_mensual
serverTodayBogota America/Bogota asistencia CONTRATOS today-only notas
DEFAULT_CARGOS Temporal cargos delete-recreate contract-schema-qa-jul-24
staging-release-qa-jul24-runbook pre-jul24-qa.sql.gz miempresa-staging
```

---

## 11) Doc index (copy-paste)
| Kind | Path |
|---|---|
| This resume | `context/resume-session/summary-2026-07-31.md` |
| Prior resume | `context/resume-session/summary-2026-07-22.md` |
| qa-jul24 cleaned feedback | `context/user-feedback/qa-session-jul-24-cleaned.md` |
| qa-jul24 contract (SSOT) | `development/qa-session-jul-24/orchestration-ctx/decisions/contract-schema-qa-jul-24.md` |
| qa-jul24 handoff | `development/qa-session-jul-24/06-handoff.md` |
| qa-jul24 implemented | `context/plan-implemented/qa-session-jul-24-implemented.md` |
| qa-jul24 staging runbook | `context/implementation-plan/staging-release-qa-jul24-runbook.md` |
| Release protocol | `.claude/skills/planify-team/release-protocol.md` |
