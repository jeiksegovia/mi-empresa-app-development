# Resume-Session Summary — 2026-07-22 (end of day / post jul-22 staging)

**Purpose**: single entry point so a **new clean context** can resume mi-empresa-app without re-deriving multi-day planify-team history.  
**Supersedes**: `summary-2026-07-17.md` (keep for jul-4→17 archaeology only).  
**Last verified**: staging health **200** (API + FE) after release `staging-release-jul22-fixes`.

---

## 0) Bootstrap (do this first in a new session)

```bash
# 1) Read this file fully
# 2) Quick live check
curl -s https://miempresa-api-stg.disruptiveexp.com/api/v1/health
curl -s -o /dev/null -w "%{http_code}\n" https://miempresa-stg.disruptiveexp.com

# 3) QA logins (custom domain ONLY — not *.amplifyapp.com)
./backend/prisma/test-db/get-qa-creds.sh --stage staging --profile disruptive --region us-east-1

# 4) Local stack (if coding)
# backend :3101 · frontend :3100 · postgres docker :15432
# admin|empleado|auditor|operador@miempresa.com / <redacted>
```

**Hard rules (never skip)**
- Commit **only** on explicit user instruction (tree is huge and multi-cycle).
- **No prod** AWS / CodeDeploy group `miempresa-prod`.
- Always AWS `--profile disruptive --region us-east-1`.
- Never `prisma migrate diff --shadow-database-url`.
- Never generic `pkill node/tsx` (local prod bun may sit on **:4142**).
- Orchestrator does **not** implement product code under planify-team; workers do.

---

## 1) State at a glance

| Layer | State |
|---|---|
| **Git** | HEAD **`f6503d7`**. **Most feature work since jul-8 is still UNCOMMITTED** (working tree >> HEAD). |
| **Local DB** | **23** migrations (latest `20260718100000_nomina_asistencia`). |
| **Staging** | **Current / shipped** through **fixes-jul-22**. BE **`d-WSU9IQ3QK`**, Amplify **job 11**, 23 mig, QA users 5/6/7, instruments TINETTI/**v2** + MNA/**v2** + VALORACION_INTEGRAL/**v1** active. |
| **Prod** | Does not exist. |

### Staging endpoints
| What | URL |
|---|---|
| FE | https://miempresa-stg.disruptiveexp.com |
| API | https://miempresa-api-stg.disruptiveexp.com/api/v1 |
| Asistencia | https://miempresa-stg.disruptiveexp.com/asistencia |
| Login | https://miempresa-stg.disruptiveexp.com/login (**custom domain only**) |

### Staging QA profiles
| Profile | Role | Use for |
|---|---|---|
| `qa-admin@miempresa.com` | ADMIN | full access |
| `qa-gerontologa@miempresa.com` | EMPLEADO + GERONTOLOGA | pacientes/fichas/instrumentos; **no** asistencia/nómina |
| `qa-contratos@miempresa.com` | EMPLEADO + CONTRATOS | empleados/nómina/asistencia/certificados; pacientes **create-only**; **no** instruments |

Passwords: SSM via `get-qa-creds.sh` (never hardcode in docs long-term).

### AWS constants
| Item | Value |
|---|---|
| Profile / region | `disruptive` / `us-east-1` |
| Instance | `miempresa-backend-staging` @ `54.144.25.72` |
| CodeDeploy | app `miempresa-app` · group **`miempresa-staging` only** |
| Amplify | `d1nsxjyualdzdu` branch `staging` |
| Backups bucket | `miempresa-backups-540657241795-staging` |
| BE artifacts | `miempresa-artifacts-540657241795-staging` |
| FE artifacts | `miempresa-frontend-artifacts-540657241795-staging` |

---

## 2) Deployed stack history (staging)

| When | Release | BE deploy | Amplify | Notes |
|---|---|---|---|---|
| jul-17 | dynamic-fichas + RBAC | `d-8E7JTGNMK` (earlier cycle) | job 9 | instruments JSON + CONTRATOS/GERONTOLOGA |
| jul-20 | **nomina-asistencia** | **`d-8OHAZOPOK`** | **job 10** | migration 23, asistencia module |
| **jul-22** | **fixes-jul-22** | **`d-WSU9IQ3QK`** | **job 11** | no new mig; instruments:upgrade v2 + FE estado/unsaved/text matrix |

**Latest staging truth = row jul-22.**

---

## 3) Feature cycles (what exists in code + docs)

### A) instrumentos-dynamic-fichas (jul-16/17) — baseline
- Versioned JSONB definitions; scoring server-side; no file plantillas.
- Contract: `development/instrumentos-dynamic-fichas/orchestration-ctx/decisions/schema-contract-instrumentos-dynamic-fichas.md`
- Templates under `backend/prisma/instrument-templates/`.
- Upgrade: `cd backend && npm run instruments:upgrade` (VERSION_LOCKED safe).

### B) RBAC fixes-jul17-2 — baseline
- `TipoEmpleado`: GERONTOLOGA | CONTRATOS.
- Matrix: `backend/src/middleware/domainAccess.ts` ↔ `frontend/app/composables/useDomainAccess.ts`.
- Contract: `development/fixes-jul17-2/orchestration-ctx/decisions/contract-fixes-jul17-2.md`.

### C) nomina-asistencia-jul-18 — staged
| Piece | Detail |
|---|---|
| Medios pago empleado | Nequi / transferencia; optional → pendiente text exact `Falta medio de pago de nómina` |
| Contrato | `valorJornada` required on create |
| Asistencia | AM/PM medias (4h); API + `/asistencia` page |
| Nómina | medias × valor; aportes only TERMINO_FIJO/INDEFINIDO; total override; dual-write `salario` |
| Domain | `asistencia` same matrix as empleados |
| Migration | `20260718100000_nomina_asistencia` |
| Handoff | `development/nomina-asistencia-jul-18/06-handoff.md` |
| Contract | `…/schema-contract-nomina-asistencia-jul-18.md` |
| Tests at close | **38 pass** |

### D) fixes-jul-22 — staged 2026-07-22
| Insight | Implementation |
|---|---|
| **I1 estado** | **Create + CONTRATOS**: hide UI; server force **ACTIVO**. **Edit**: only **ADMIN/GERONTOLOGA** (`PATIENT_STATE_FORBIDDEN`). |
| **I2 unsaved** | `frontend/app/composables/useUnsavedGuard.ts` on patients + instruments forms |
| **I3 MNA** | `MNA_CUADRO.v2.json` `cellInput:"text"`; FE text grid |
| **I4–I5 Tinetti** | `TINETTI.v2.json` `eq_vuelta_360` + `ma_pie_derecho`/`ma_pie_izquierdo`; max total **27** |
| **I6 valoración** | `VALORACION_INTEGRAL.v1.json` from xlsx sheet **VALORACIÓN INTEGRAL** only |
| Handoff | `development/fixes-jul-22/06-handoff.md` |
| Contract | `…/schema-contract-fixes-jul-22.md` |
| Local QA | **62 pass / 0 fail** |
| Staging release | `context/implementation-plan/staging-release-jul22-fixes-runbook.md` · `d-WSU9IQ3QK` · Amplify job **11** |

---

## 4) Authoritative contracts (SSOT — do not invent names)

| Topic | Path |
|---|---|
| Instruments base | `development/instrumentos-dynamic-fichas/orchestration-ctx/decisions/schema-contract-instrumentos-dynamic-fichas.md` |
| RBAC | `development/fixes-jul17-2/orchestration-ctx/decisions/contract-fixes-jul17-2.md` |
| Nómina/asistencia | `development/nomina-asistencia-jul-18/orchestration-ctx/decisions/schema-contract-nomina-asistencia-jul-18.md` |
| Jul-22 fixes | `development/fixes-jul-22/orchestration-ctx/decisions/schema-contract-fixes-jul-22.md` |

---

## 5) Key source paths (fast map)

| Area | Paths |
|---|---|
| Patient estado | `backend/src/services/patientService.ts`, `patients.routes.ts`; FE `pacientes/crear.vue`, `[id]/editar.vue` |
| Unsaved guard | `frontend/app/composables/useUnsavedGuard.ts` |
| Instruments templates | `backend/prisma/instrument-templates/{TINETTI,MNA_CUADRO}.v2.json`, `VALORACION_INTEGRAL.v1.json` |
| Scoring / text cells | `backend/src/services/instrumentScoringService.ts`; FE `DynamicGroupInfoField.vue` |
| Asistencia | `backend/src/services/asistenciaService.ts`, `routes/asistencia.routes.ts`; FE `pages/asistencia/index.vue` |
| Domain RBAC | `backend/src/middleware/domainAccess.ts`, `frontend/app/composables/useDomainAccess.ts` |
| Seed QA | `backend/prisma/test-db/seed-qa-staging.sh`, `get-qa-creds.sh` |
| FE deploy | `frontend/infrastructure/scripts/deploy-frontend.sh` |
| DB reset | `backend/infrastructure/db/scripts/reset-staging-db.sh` |

---

## 6) Verification commands

```bash
# Local BE jul-22
cd backend && npx playwright test \
  tests/patients/patient-estado-rbac.spec.ts \
  tests/instruments-dynamic/fixes-jul-22-templates.spec.ts \
  tests/instruments-dynamic/fixes-jul-22-api.spec.ts \
  tests/instruments-dynamic/scoring-engine.spec.ts \
  tests/instruments-dynamic/seed-definitions.spec.ts --reporter=list

# Local FE jul-22
cd frontend && npx playwright test tests/fixes-jul-22 --reporter=list

# Nomina/asistencia
cd backend && npx playwright test \
  tests/employees/medio-pago*.spec.ts tests/asistencia/ tests/nomina/nomina-calc-*.spec.ts --reporter=list

# After template changes locally
cd backend && npm run instruments:upgrade
```

FE e2e often needs host match: `TEST_FRONTEND_URL` / `TEST_API_URL` (e.g. Tailscale IP, not bare localhost cookies).

---

## 7) Staging release playbook (next time)

Pattern proven jul-18 (wipe) and jul-22 (additive):

1. Write runbook under `context/implementation-plan/staging-release-*-runbook.md`
2. R0 preflight → R1 backup → R2 wipe **only if** schema reset needed → R3 seed-qa → R4 CodeDeploy **miempresa-staging** → R5 Amplify → R6 canary  
3. If instrument JSON changes without migration: **R4 must include templates + `npm run instruments:upgrade` on instance**  
4. seed-qa: B34/B35 already fixed (`--overwrite`, no `declare -A`)  
5. Auto-chain only when user says “execute end-to-end”; otherwise PROCEED gates  
6. Monitor via worker CHECKPOINT/COMPLETE messages — **do not bash-sleep poll** (orchestration protocol)

Latest additive example: `staging-release-jul22-fixes-runbook.md`  
Latest wipe example: `staging-release-jul18-nomina-asistencia-runbook.md`

---

## 8) Open / next (pick one)

1. **Git commit** (user must ask) — enormous multi-cycle WT  
2. **New QA / features** from next raw feedback under `context/user-feedback/`  
3. **More FORMATOS DE INGRESO sheets** (only VALORACIÓN INTEGRAL built)  
4. Pre-existing debt: certificates list filter 500; print CSS; etc.  
5. Optional live browser canary of unsaved-guard + MNA text matrix on staging UI  

---

## 9) Orchestration lessons (carry forward)

- Contract-first multi-worker: backend → frontend → QA; QA tests contract not implementation.
- Staging: prefer artifact CHECKPOINT/COMPLETE over sleep loops.
- Shutdown: structured `shutdown_request`; if process lingers after `approve:true`, **kill exact PID only**.
- Never free worker name while degraded; never pkill node.
- Agent Teams env: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`; spawn from **repo root**.

---

## 10) Grep hooks

```
resume-session jul-22 f6503d7 23-migrations
d-WSU9IQ3QK amplify-job-11 d-8OHAZOPOK amplify-job-10
PATIENT_STATE_FORBIDDEN eq_vuelta_360 ma_pie_derecho cellInput VALORACION_INTEGRAL
useUnsavedGuard hideEstadoCreate canEditEstado
nomina-asistencia AsistenciaEmpleado valorJornada
seed-qa-staging B34 B35 OP-7 miempresa-staging
schema-contract-fixes-jul-22 schema-contract-nomina-asistencia-jul-18
```

---

## 11) Doc index (copy-paste)

| Kind | Path |
|---|---|
| This resume | `context/resume-session/summary-2026-07-22.md` |
| Prior resume | `context/resume-session/summary-2026-07-17.md` |
| Nomina handoff | `development/nomina-asistencia-jul-18/06-handoff.md` |
| Fixes-22 handoff | `development/fixes-jul-22/06-handoff.md` |
| Staging jul-22 runbook | `context/implementation-plan/staging-release-jul22-fixes-runbook.md` |
| Staging jul-18 runbook | `context/implementation-plan/staging-release-jul18-nomina-asistencia-runbook.md` |
| Deploy checklist | `context/implementation-plan/staging-deploy-checklist.md` |
| Implemented notes | `context/plan-implemented/{nomina-asistencia-jul-18,fixes-jul-22,staging-release-jul22-fixes}-implemented.md` |
