# Resume-Session Summary — 2026-07-17

**Purpose**: condensed state of mi-empresa-app so any session resumes efficiently. Supersedes
`summary-2026-07-11.md` (still valid for jul-4→11 history). Covers the three jul-16/17 cycles:
instrumentos-dynamic-fichas, staging release jul-17, fixes-jul17-2 (RBAC + QA users).

## Current state at a glance
| Layer | State |
|---|---|
| Git | HEAD `a169460` (jul 8-10 work). **Everything since is UNCOMMITTED** (~2 full feature cycles in the working tree: dynamic-fichas + fixes-jul17-2). Commit only on explicit instruction |
| Local dev | backend :3101 (tsx watch), frontend :3100, pg :15432 docker. Local DB: 22 migrations, seeded. Dev logins: admin/empleado/auditor/operador@miempresa.com / <redacted> |
| Staging | **FULLY SHIPPED + CLEAN**. Backend CodeDeploy `d-8E7JTGNMK`, Amplify job 9, 22/22 migrations, canonical seed + 3 QA users, uploads bucket wiped (recoverable backups exist). Domains: https://miempresa-stg.disruptiveexp.com + api `miempresa-api-stg.disruptiveexp.com/api/v1` |
| Prod | Does not exist. Global approval rule applies |

## What was built (jul-16/17)

### 1. instrumentos-dynamic-fichas (BREAKING — instruments are no longer files)
- Definitions = versioned JSONB (`instrumentos_versiones`, immutable once referenced, one activo
  per instrument via partial unique index). Fichas store `respuestas` JSONB + server-computed
  `subtotales`/`puntajeTotal(Float)`/`clasificacion`. Migration `20260717045038` (hard reset, notas preserved).
- 6 seeded instruments (specs `context/instrumentos-depurated/`, templates
  `backend/prisma/instrument-templates/*.v1.json`): BARTHEL(100), MINI_MENTAL(30), TINETTI(28),
  YESAVAGE(15), MNA_CUADRO(30, conditional MAY-skip cribaje≥12 with section-level classification),
  FICHA_NUTRICIONAL (informational). 5 item types (`single-select-scored`, `number-info`,
  `text-info`, `single-select-info`, `group-info`).
- Engine `backend/src/services/instrumentScoringService.ts` (pure; client scores never trusted;
  G2-11 server resolves instrumentoVersionId; G2-12 versionRegistro server-derived).
- API: GET /instruments (+activeVersion), GET /instruments/:codigo/definition, POST
  /patients/:id/fichas (respuestas optional → PENDIENTE assign kept), PATCH .../:fichaId/completar,
  GET .../:fichaId with scores. File flow (plantilla/archivoCompletado) REMOVED.
- Frontend: `components/instrument/{DynamicInstrumentForm,DynamicSection,DynamicItemField,
  DynamicGroupInfoField,InstrumentResultView,InstrumentAuditView}.vue` + `scoring.ts` (optimistic) +
  `/dev/instrument-preview`. `npm run instruments:upgrade` (VERSION_LOCKED protection).
- AUTHORITATIVE CONTRACT: `development/instrumentos-dynamic-fichas/orchestration-ctx/decisions/schema-contract-instrumentos-dynamic-fichas.md`

### 2. RBAC (fixes-jul17-2)
- `TipoEmpleado {GERONTOLOGA, CONTRATOS}` (migration `20260717120000`). Matrix single-source:
  `backend/src/middleware/domainAccess.ts` (`DOMAIN_ACCESS` + `requireDomain`, 403
  `DOMAIN_FORBIDDEN`), mirrored in `frontend/app/composables/useDomainAccess.ts` (parity QA-tested).
- Profiles: gerontóloga = pacientes/fichas/instrumentos/notas; contratos = empleados/nómina/
  certificados + pacientes CREATE-ONLY (method-level); empresa = neither. ADMIN bypass;
  tipoEmpleado=null EMPLEADO = legacy access (zero regression). tipoEmpleado in login//me payloads.
- Frontend: sidebar/route-middleware gating + toast; fichas tab / notas / Editar gated.
- CONTRACT ADDENDUM: `development/fixes-jul17-2/orchestration-ctx/decisions/contract-fixes-jul17-2.md`

### 3. Instrumentos UX (fixes-jul17-2)
- Crear-from-template: POST /instruments `templateCodigo` → deep-copy of template's active
  definition as the new instrument's own v1 (source byte-immutable). Sin-definición instruments:
  badge + excluded from assign picker.
- `InstrumentAuditView` (all option scores/ranges/skip rules, print-friendly) + "Probar sin
  guardar" dry-run (live client scoring, ZERO network writes) on instrument details.

### 4. QA users + seeding discipline
- `backend/prisma/test-db/`: seed-qa.ts upserts 3 profiles; seed-qa-staging.sh ensures SSM pairs
  `/miempresa/staging/qa/{qa-admin,qa-gerontologa,qa-contratos}/{EMAIL,PASSWORD}` (+ legacy alias);
  `get-qa-creds.sh` prints all three. Staging user ids: qa-admin=5, qa-gerontologa=6, qa-contratos=7.
- **RULE (OP-7)**: after ANY staging deploy/reset, `seed-qa-staging.sh` is REQUIRED (QA users are
  NOT in db:seed — skipping it breaks staging login; that was the jul-17 incident). Checklist:
  `context/implementation-plan/staging-deploy-checklist.md`.
- New destructive-op utility: `backend/infrastructure/scripts/wipe-staging-s3.sh` (staging-only,
  TTY+typed phrase, dry-run default, manifest). `reset-staging-db.sh` extended (canonical seed).

## Tests (all green at close)
Backend: `tests/rbac/` (13), `tests/instruments-dynamic/` (48+ incl. create-from-template),
patients/instruments regression 114/0 (legacy suites modernized to answers-based contract).
Frontend: `tests/rbac/` (nav-gating 5 + live-profiles 9), `tests/instruments-dynamic/`
(schema-render 7, e2e 6, fill-flow 4, crear-template, audit-dryrun). Env:
`TEST_FRONTEND_URL=http://100.85.193.33:3100 TEST_API_URL=http://100.85.193.33:3101/api/v1` +
origin-aware `tests/helpers/auth.ts`; PrimeVue RadioButton needs `page.evaluate` click.

## Staging ops references
- Runbooks: `staging-release-jul17-runbook.md` (dynamic-fichas release, OP-1..OP-7 + B27-B33) and
  `staging-release-jul17-2-runbook.md` (RBAC release, B34/B35). READ trap lists before any deploy.
- Backups: DB dumps `pre-releases/pre-jul17{,-2}.sql.gz` + wiped-S3 byte backup
  `pre-releases/s3-objects/uploads-staging-jul17/` (47 objects) in `miempresa-backups-…-staging`.
- Known traps: B27 expect-PTY for TTY-guarded scripts; B29 name helpers note() not log(); B30
  ssh-keyscan; B33 pg_dump PATH `/opt/homebrew/opt/postgresql@16/bin`; OP-3 canonical creds after
  resets; SSM qa params are the ONLY QA-cred source of truth.

## Open items / follow-ups
1. **Uncommitted working tree** (2 cycles) — commit only on explicit instruction.
2. B34 (seed-qa-staging.sh `declare -A` vs macOS bash 3.2) + B35 (put_param lacks --overwrite,
   non-idempotent) — small fix-up wave pending.
3. B32: add TTY-guard to reset-staging-db.sh (confirm is currently pipeable).
4. Pre-existing: certificates list filter 500 (`certificates.spec.ts:108`); 4 fixture-chain skips
   in instruments.spec.ts; print CSS for InstrumentResultView.
5. Orchestration convention this session (user directive): FRESH workers only, structured shutdown
  after validation, no parked reuse. planify-team artifacts under `development/{instrumentos-dynamic-fichas,fixes-jul17-2}/`.

## Grep hooks
resume-session jul-17 a169460 22-migrations instrumentos-dynamic-fichas InstrumentoVersion
respuestas puntajeTotal skipIf G2-11 G2-12 rbac CONTRATOS DOMAIN_FORBIDDEN qa-admin qa-gerontologa
qa-contratos get-qa-creds seed-qa-staging OP-7 staging-deploy-checklist d-8E7JTGNMK amplify-job-9
wipe-staging-s3 B34 B35 crear-from-template templateCodigo InstrumentAuditView probar-sin-guardar
