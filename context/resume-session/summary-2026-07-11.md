# Resume-Session Summary — 2026-07-11

**Purpose**: condensed state of mi-empresa-app so any session can resume efficiently. Covers everything implemented jul-4 → jul-11 and the live state of backend / DB / frontend / infrastructure.

---

## Current state at a glance

| Layer | State |
|---|---|
| Git | HEAD `48029ef` (last commit = jul-7/8 work). **~146 files uncommitted** spanning jul-9, jul-10, hotfixqa, I1-I3 cycles. Draft commit message exists (see §Pending). Snapshot tags: `staging-jul5-snapshot`, `staging-jul10-snapshot` |
| Local dev | backend `:3101` (tsx watch, `cd backend && npm run dev`), frontend `:3100` (nuxt, host 0.0.0.0, LAN IPs allowed), pg `:15432` (docker). Admin: `admin@miempresa.com/<redacted>`. `frontend/.env` points apiBase at `http://100.85.193.33:3101/api/v1` (LAN QA) — sameSite=strict means playwright must use matching hosts |
| DB (local) | 20 migrations applied, schema up to date (`miempresa_dev`) |
| Staging | Fully deployed through the hotfixqa cycle + (in-flight) I1-I3 deploy. Backend CodeDeploy `miempresa-app`/`miempresa-staging` on Lightsail `54.144.25.72`; frontend Amplify (last Job 6); DB 20/20 migrations; empresa id=1 + 7 cargos seeded live |
| Prod | **Does not exist yet / never touched.** All prod-named resources require explicit approval (global rule) |

## Implemented, by cycle (chronological)

1. **jul-5 storage validation** — S3-only storage confirmed; CRITICAL-1 (nonexistent local bucket → dev bucket `miempresa-uploads-540657241795-dev` created) + CRITICAL-2 (`useFileUpload` res.data unwrap) fixed; CORS scoped; staging release runbook pattern established. `context/implementation-plan/storage-validation-report-jul5.md`
2. **jul-8 fixes (5 domains)** — CertificadoUpdate history model + endpoints; instrumentos roles Zod + MultiSelect + editar page; fichas 3 bugs + VENCIDO→COMPLETADO; nomina contract filter + cuenta-cobro enforcement; Android tab-discard root cause → `useFileStash` (IndexedDB) on 5 upload forms; 401 UX via `session-expired.client.ts`. `context/plan-implemented/initial-prompt-fixes-jul-8-implemented.md`
3. **jul-9 release to staging** — runbook `staging-release-jul9-runbook.md`, deployment `d-M8XBER0HK`, Amplify Job 2→3
4. **improvements-jul-9 (Sprint 1+2, 17 items from QA transcript)** — cert create simplified + shared `CertificateUpdateForm` + comprobantePago on updates; empresa save bug; notas `fechaIncidente` + 2-business-day hard block (`businessDays.ts`, weekday-only); cliente `fechaCumpleanos/tipoSangre/eps`; `EducacionEmpleado` table + CRUD; `documentoIdentificacionUrl`; contrato-laboral tab + `archivoFirmadoUrl`; `CargoEmpresa` catalog (per-empresa, 7+Otro seed) + cargos manager UI. `context/plan-implemented/improvements-jul-9-implemented.md`
5. **next-release-jul-10** — fichas **single-step** assign+complete (atomic `POST /patients/:id/fichas`, legacy PENDIENTE flow kept); descargar plantilla renamed `{instrumento}_{paciente}`; C3 crear-instrumento shortcut; **lazy PENDIENTE→VENCIDO flip** (no cron); `GET /patients/fichas/vencimientos`; `TipoEmpleado` enum (GERONTOLOGA) + `requireInstrumentWriter` gating + `/users` CRUD (tipoEmpleado pairing rule); D7 tighten (`cargoId` NOT NULL + required); **E1 uppercase** (Zod transform + as-you-type on nombre fields, 5 models); hardening: jul4 suite resurrected 2/24→24/24, port-3001 fixes, D3 spec. `context/plan-implemented/next-release-jul-10-implemented.md`
6. **jul-10 staging release** — 6 migrations applied 14→20, `d-9CDIOTWHK`, Amplify Job 4, 33/33 QA. Runbook `staging-release-jul10-runbook.md`
7. **W6 empresa-bootstrap hotfix** — `POST /empresa` (single-empresa 409, atomic 7-cargo seed), GET normalized `data:null`, frontend create-mode; deployed `d-NT3SGP4IK` + Job 5; staging empresa id=1 created live
8. **hotfixqa-jul-10 incident** — "token expirado" everywhere = **SDK credential-pinning** (memoizeChain never invalidates file creds without `expiration`; PM2 signs with startup-cached STS creds while cron rotates the file). FALSE regression — latent since STS design. Fixes: pm2 restart → **stopgap-B** (guarded pm2 reload in `refresh-credentials.sh`, instance + repo) → **P0** `backend/src/config/awsCredentials.ts` (env-aware provider, mtime+55min synthesized expiration; local default-chain untouched). R3b isolation proof. Also: S7 (empleado-cert `archivoUrl` dropped from PUT) fixed; **`useFileUpload` PUT-status check** added (failed uploads used to return keys as success); UI items (firmado download, pausa tooltip, contrato tab on detail, silent-failure surfacing). QA hardening: `assertUploadPersisted`, S3 canary spec, silent-failure standard, staging coverage. Deployed `d-VGKFFBAIK` + Amplify Job 6; staging hardening 7/7. `context/plan-implemented/hotfixqa-jul-10-implemented.md`
9. **I1-I3 presign hardening (jul-11, ✅ DEPLOYED)** — `CREDS_EXPIRED` 503 fail-fast before signing; expiry clamp to cred lifetime; download default 3600→**900s** (closes MED-2). 51/51 backend tests. Staging deploy `d-HH2LFJIIK` (artifact `i123-20260711-084102.zip`, backup `pre-i123.sql.gz`); clamp OBSERVED live (670→645s late in session window) + default 900 verified on fresh creds; PUT→GET round-trip byte-identical; 8-endpoint smoke clean; pm2 pid 510987. I1 503-path covered by unit tests (live cron-pause test deliberately NOT run — would break staging ~1h). Analysis: `development/hotfixqa-jul-10/orchestration-ctx/decisions/presign-security-analysis.md`; deploy log: `tasks/W12-presign-hardening/deploy-result.md`

## Backend state
Express + Prisma (v6, `src/generated`), Zod-at-top-of-routes, service→route pattern, structured errors `{success:false,message,field/code}`. Notable modules: `services/{certificateService,patientService,nominaService,employeeService,cargoEmpresaService,educacionEmpleadoService}`, `middleware/{auth(requireRole,requireInstrumentWriter),forbidLegacy}`, `utils/businessDays.ts`, `config/awsCredentials.ts`, `services/s3Service.ts` (presign, fail-fast+clamp). CloudFront `x-origin-verify` header gate in app.ts. Tests: playwright API specs under `backend/tests/{area}/` (~280 passing; known pre-existing fails: certificates flake, employees-full-create certificadoAlturas BUG, sub-resources 500-assert, staging-smoke env-blocked).

## DB state (20 migrations)
Key models added since jul-8: `CertificadoUpdate` (append-only history; parent = snapshot), `EducacionEmpleado`, `CargoEmpresa` (per-empresa, unique nombre, soft-archive `activo`), enums `TipoSangre`, `TipoEmpleado`; `NotaCliente.fechaIncidente` NOT NULL; `Contrato.cargoId` NOT NULL FK; `Cliente.{fechaCumpleanos,eps,tipoSangre}`; `Empleado.{documentoIdentificacionUrl,nivelEscritura?}`; `Usuario.tipoEmpleado`. Fichas transitions: PENDIENTE→{COMPLETADO,VENCIDO}, COMPLETADO→VENCIDO, VENCIDO→COMPLETADO. NEVER run `migrate diff --shadow-database-url`.

## Frontend state
Nuxt 4 SPA (`ssr:false`), PrimeVue auto-import, Pinia. Key composables: `useApi` (401 → DOM event → `session-expired.client.ts` plugin), `useFileUpload` (res.data unwrap + **PUT ok-check**), `useFileStash` (IndexedDB, row-scoped keys `entity:<id>:file`). Shared `components/certificate/CertificateUpdateForm.vue`. Uppercase-as-you-type on entity nombre inputs. Tests: `frontend/tests/local-qa/` (jul4/8/9/10 + w11 suites), `tests/e2e/`, `tests/staging/` (canary + coverage), helper `tests/helpers/auth.ts` (origin-aware; REQUIRED because sameSite=strict). Known env quirk: run with `TEST_FRONTEND_URL=http://100.85.193.33:3100 TEST_API_URL=http://100.85.193.33:3101/api/v1`.

## Infrastructure state
- **Stacks (us-east-1, profile `disruptive` — ALWAYS pass `--region us-east-1`)**: `miempresa-s3-{dev,staging}`, `miempresa-iam`, `miempresa-ssm-staging`, `miempresa-codedeploy`, `miempresa-edge-staging`, `miempresa-frontend-staging`
- **Deploy pipeline**: local build → zip (appspec at root, NO dist/generated — `after-install.sh` regenerates on-instance) → S3 artifacts → CodeDeploy `miempresa-app`/`miempresa-staging` → migrations run on-instance. Frontend: `nuxt generate` → zip contents of `.output/public` → Amplify start-deployment
- **Credentials architecture**: bootstrap IAM user → STS AssumeRole cron (`refresh-credentials.sh` :00/:45, 1h sessions) → files for root + ec2-user → PM2 (ec2-user). **Two protections against cred-pinning**: P0 provider (code) + stopgap pm2-reload (script, instance AND repo copies — keep both)
- **Buckets**: uploads `miempresa-uploads-540657241795-{dev,staging}` (CORS: staging domain + localhost 310x + dev IPs 100.85.193.33/10.57.126.228 on dev), artifacts, backups (pre-release dumps under `pre-releases/`), frontend-artifacts
- **Runbooks**: `context/implementation-plan/staging-release-jul{5,9,10}-runbook.md` (jul-10 one has hotfix sections appended) + `development/fixes-jul-8/tasks/W10-staging-release-jul9/worker-deploy-learning.md` (trap list — READ BEFORE ANY DEPLOY)

## Pending / open items
1. **Working tree uncommitted (~146 files)** — commit only on explicit instruction; draft message covering jul-7→10 exists in session history; extend for hotfixqa + I1-I3
3. **I4** (`ResponseContentDisposition` filenames on downloads — closes MED-3) — evaluated, not implemented
4. **Deferred features**: C5 TipoInstrumento enum (blocked on client Excel), user-management UI (tipoEmpleado is API-only), on-instance cron canary, GAP-1 legacy spec origin sweep (8 tests, 5 files), employees-full-create certificadoAlturas pre-existing BUG, Colombian holiday table
5. **Staging carry-overs**: `jul10_contrato_cargo_not_null` migration hardcodes empresa_id=6 (dev-tuned; re-tune before prod), staging test entities soft-deleted (INACTIVO)
6. **Prod readiness note**: P0 provider + stopgap block + edge/origin-verify must be in prod instance design from day one

## Orchestration conventions (planify-team)
`development/{slug}/` with orchestration-ctx (team-plan/team-status/decisions) + tasks/W{N}. Lessons that MUST be honored: never reuse teammate names while shutdowns pending; parked workers don't survive session change (PING before reuse); schema-contract-first for multi-worker; tests are a wave, not optional; validate deliverables on disk before accepting COMPLETE; time-dependent behavior needs boundary testing (the credential incident). Session teams status files: `development/{fixes-jul-8,improvements-jul-9,next-release-jul-10,hotfixqa-jul-10}/orchestration-ctx/team-status-*.md`.

## Grep hooks
resume-session jul-11 state-snapshot 48029ef 20-migrations d-VGKFFBAIK amplify-job-6 CREDS_EXPIRED 900s stopgap-B awsCredentials cargoEmpresa fichas-single-step vencimientos TipoEmpleado uppercase-E1 assertUploadPersisted canary worker-deploy-learning
