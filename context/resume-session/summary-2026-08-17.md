# Resume-Session Summary — 2026-08-17 (jul-31 → aug-6 followup shipped)

**Purpose**: single entry point so a **new clean context** can resume mi-empresa-app without re-deriving history.
**Supersedes**: `summary-2026-07-31.md` (keep for jul-24 archaeology).
**Last verified**: staging health **200** after `staging-release-qa-aug6-followup` (CodeDeploy **`d-13RWC0Q4L`**, Amplify **job 15**). Staging migrations **28**.

---

## 0) Bootstrap (do this first in a new session)

```bash
# 1) Read this file fully
# 2) Quick live check
curl -s https://miempresa-api-stg.disruptiveexp.com/api/v1/health
curl -s -o /dev/null -w "%{http_code}\n" https://miempresa-stg.disruptiveexp.com
# 3) QA logins (custom domain ONLY — not *.amplifyapp.com)
./backend/prisma/test-db/get-qa-creds.sh --stage staging --profile disruptive --region us-east-1
# 4) Local stack: backend :3101 · frontend :3100 · postgres docker :15432
#    admin|empleado|auditor|operador@miempresa.com / <redacted>
```

**Hard rules (never skip)**
- Commit **only** on explicit user instruction. Developer always commits himself. Agent may `git add` specific paths when asked; **never `git commit`**.
- **No prod** AWS. CodeDeploy group `miempresa-prod` EXISTS — never target it (`miempresa-staging` only).
- Always AWS `--profile disruptive --region us-east-1`.
- Never `prisma migrate diff --shadow-database-url`.
- Never generic `pkill node/tsx` (local prod bun may sit on **:4142**). Restart local BE via `lsof -ti :3101` then `PORT=3101 npm run dev`.
- Orchestrator (planify-team) does **not** implement product code; workers do.
- **Migrations are gitignored** → they ship inside the CodeDeploy artifact (packages **working tree**), applied on-instance via `npx prisma migrate deploy`.
- **Prefer existing tooling** before improvising VM/SSH/debug: `backend/infrastructure/db/utilities/ssh-to-instance.sh`, key `~/.ssh/miempresa-lightsail-key.pem`. Staging is **Lightsail** (not SSM EC2). Memory: `prefer-existing-tooling-over-improvising`.
- Working-tree CodeDeploy ships **everything dirty** (not just the cycle). Check ride-alongs before R4 (aug-6 shipped centro-costos ago-5 because of this).

---

## 1) State at a glance

| Layer | State |
|---|---|
| **Git** | Branch `main`, HEAD **`8e07055`** ("Fixes from jul 31 to aug 6: instrument fields, permissions on qa-contrator role on empleados, lock employee…"). **Partial commit** — 17 files (templates v1/v2, TINETTI v4/v5, nomina contratos guard, instruments-upgrade, crear.vue). **~119 files still UNCOMMITTED** (lock, EPS/ARL, nomina valorMensual dialog, RBAC PROFESORES/AUXILIARES, domainAccess, schema, centro-costos, most tests/docs). |
| **Local DB** | **28** migrations (newest `20260806035159_add_tipoempleado_profesores_auxiliares`). |
| **Staging** | **Current through qa-aug-6 followup.** BE **`d-13RWC0Q4L`**, Amplify **job 15**, 28 mig. TINETTI **v5** active; SIGNOS/BOLETIN **v2** active. |
| **Prod** | Does not exist. |

### Staging endpoints
| What | URL |
|---|---|
| FE | https://miempresa-stg.disruptiveexp.com |
| API | https://miempresa-api-stg.disruptiveexp.com/api/v1 |
| Login | https://miempresa-stg.disruptiveexp.com/login (**custom domain only** — SameSite=strict cookie) |

### Staging QA profiles (passwords via SSM / `get-qa-creds.sh`)
| Profile | Role | Scope (current matrix) |
|---|---|---|
| `qa-admin@miempresa.com` | ADMIN | full; only role that can lock/unlock empleados |
| `qa-gerontologa@miempresa.com` | EMPLEADO + GERONTOLOGA | pacientes/fichas/instrumentos/**certificados=true**; notas full; no empleados/nómina/asistencia |
| `qa-contratos@miempresa.com` | EMPLEADO + CONTRATOS | empleados/nómina/asistencia/certificados; pacientes create-only; **can CRUD contratos when empleado unlocked**; asistencia PUT = today only |

No dedicated QA users yet for PROFESORES / AUXILIARES (enum exists; seed-qa not extended).

### AWS constants
| Item | Value |
|---|---|
| Profile / region | `disruptive` / `us-east-1` |
| Instance | Lightsail `miempresa-backend-staging` @ `54.144.25.72` (ec2-user, `/opt/miempresa/app`, pm2 `miempresa-api`) |
| SSH | `~/.ssh/miempresa-lightsail-key.pem` + `backend/infrastructure/db/utilities/ssh-to-instance.sh` |
| CodeDeploy | app `miempresa-app` · group **`miempresa-staging` only** |
| Amplify | `d1nsxjyualdzdu` branch `staging` |
| Buckets | backups `miempresa-backups-540657241795-staging` · BE `miempresa-artifacts-…-staging` · FE `miempresa-frontend-artifacts-…-staging` |

---

## 2) Deployed stack history (staging)

| When | Release | BE deploy | Amplify | Notes |
|---|---|---|---|---|
| jul-20 | nomina-asistencia | `d-8OHAZOPOK` | job 10 | mig 23 |
| jul-22 | fixes-jul-22 | `d-WSU9IQ3QK` | job 11 | instruments v2 + FE |
| jul-31 | qa-session-jul-24 | `d-OJON0UVVK` | job 12 | mig 24; EFECTIVO, valor_mensual, cargos→Temporal |
| aug-04 | qa-jul31 + lock + TINETTI v3 | `d-EDEN0GJYK` | job 13 | mig 25–26 (eps/arl + bloqueado); TINETTI v3 then v4/v5 via upgrade |
| aug-06 | fixes-features-aug-6 | `d-MR1QVXIZK` | job 14 | mig 27–28 (centro-costos + PROFESORES/AUXILIARES); SIGNOS/BOLETIN v1 |
| **aug-14** | **qa-aug-6 followup** | **`d-13RWC0Q4L`** | **job 15** | CONTRATOS contratos CRUD; SIGNOS/BOLETIN **v2**; no new mig |

**Latest staging truth = row aug-14 (`d-13RWC0Q4L` / job 15).**

Backups: `pre-jul24-qa.sql.gz` · `pre-jul31-aug04.sql.gz` · `pre-aug6.sql.gz` · `pre-aug6-followup.sql.gz`.

---

## 3) Cycles since jul-31 (what shipped)

### 3.1 qa-session-jul-31 (planify-team, 2 workers)
Source: `context/user-feedback/qa-session-jul-31-{raw,cleaned}.md`
Contract: `development/qa-session-jul-31/orchestration-ctx/decisions/contract-schema-qa-jul-31.md`

| # | Item | Key impl |
|---|---|---|
| R1 | Nequi → **Nequi/Bre-B** label | Display only; enum `NEQUI` unchanged. 4 surfaces: empleados nuevo/editar/detail + nomina |
| R2 | Nómina Registrar adapts to tipoContrato | FE `usaValorMensual` in `frontend/app/pages/nomina/index.vue`. OPS = medias×valorJornada; **OBRA/FIJO/INDEF = valorMensual**. Aportes only FIJO/INDEF (`APORTES_ALLOWED`). |
| R2b | BE suggestion already had valorMensual | **No new BE** (jul-24 R7). Read `sugerido.valorMensual` / `contratoActivo.valorMensual` |
| R3 | EPS / Fondo pensiones / ARL | `empleados.eps`, `fondo_pensiones`, `arl` VARCHAR(100) nullable. Migration `20260803022244_add_empleado_eps_fondo_arl` |

**D1** OBRA uses valorMensual, **no aportes**. **D2** free-text, no catalog. **D5** centros de costos deferred (later built as ago-5).

Tests: `backend/tests/employees/empleado-eps-fondo-arl.spec.ts`, `backend/tests/nomina/nomina-sugerencia-valormensual.spec.ts`, `frontend/tests/local-qa/jul31-qa-frontend.spec.ts` (13 mocked).

### 3.2 aug-04 followup (same task dir `development/qa-session-jul-31/`)
Docs: `followup-aug-04-plan.md`, `followup-aug-04-completion.md`, `fixes-aug-04-test-flake-and-rbac-session.md`

| # | Item | Key impl / lesson |
|---|---|---|
| Test flake | jul-24 contrato-monthly timeout | **Test defect**: OBRA requires `fechaFin`; test never filled `contrato-fecha-fin`. App OK. Fixed in `frontend/tests/local-qa/jul24-qa-frontend-contract-monthly.spec.ts` |
| Stale session | gerontologa leftover blocked `/empleados` | Client-side `domain-access.global.ts` reads Pinia identity. Staging logs: **zero** employees 403; gerontologa login preceded. Fix: `auth.ts` `login()` resets user/empresa/isAuthenticated **first**. Deleted dead `backend/src/routes/auth.routes.ts` (cookie name `sessionToken` vs live `session`) |
| Lock (bloqueador) | Admin lock sensitive employee | Schema `bloqueado` / `bloqueado_por` / `bloqueado_en`. Migration `20260804165919_add_empleado_bloqueado`. Middleware `requireEmployeeUnlocked` in `domainAccess.ts`. Endpoints `PUT /employees/:id/lock` + `/unlock` (`requireRole('ADMIN')`). `bloqueado*` **absent from update Zod** → stripped. FE: list icon, detail badge+toggle, editar banner. |
| TINETTI | gait item 11 | **v3** combined 4-opt (wrong mix) → **v4** clinical 4 binary sub-items → **v5** (user literal): 11a/11b single-select **4 opts A/B/C/D scored 0/1/0/1**. Max 27→**25**; thresholds **bajo 23–25 · mod 17–22 · alto 0–16**. Active on staging = **v5**. |

**Lock scope note**: contratos routes were still ADMIN-only then; lock on PUT/DELETE `/employees/:id` covered payment. Later (aug-6 followup) contratos mutations also got `requireEmployeeUnlocked`.

**TINETTI lesson**: `computeScore()` returns **`puntajeTotal`**, not `.total`. v3/v4 specs were vacuously `undefined===undefined` until fixed.

### 3.3 fixes-features-aug-6 (planify-team, 3 workers)
Dir: `development/fixes-features-aug-6/`
Contract: `orchestration-ctx/decisions/contract-fixes-features-aug-6.md`
Handoff: `06-handoff.md`

| # | Item | Key impl |
|---|---|---|
| S1 | Roles PROFESORES + AUXILIARES | `TipoEmpleado` +2 values. Migration `20260806035159_add_tipoempleado_profesores_auxiliares`. Identical matrix: pacientes=`read-only` (new value), fichas/notas=`create-only`, rest false. FE `isReadOnly()`. Notes: LIST `autor=userId`; PUT/DELETE 403; admin+geronto see all. |
| S2 | SIGNOS_VITALES + BOLETIN_ANUAL | Informational (`scoring.total='none'`). Seeded via `instruments:upgrade` reading template top-level `rolesPermitidos`. |
| S3 | GERONTOLOGA certificados | Matrix cell `false`→`true` (BE+FE). |
| S4 | Gerontologa create instrument | **Contract §3.1 hypothesis was WRONG** (caller CSV `EMPLEADO,GERONTOLOGA` intersects seed `ADMIN,EMPLEADO` on EMPLEADO). Real causes: (a) crear.vue `rolesPermitidos` default now includes creator; (b) **codigo required** when template selected — else `codigo=null` and `[id]/index.vue` fetches `/instruments/:codigo/definition` → "Sin definición — no llenable". BE still shipped explicit ADMIN bypass + token trim/upper + create-default. |

**`read-only`**: GET only; POST/PUT/PATCH/DELETE → 403 `DOMAIN_FORBIDDEN`.
**`rolesPermitidos`**: CSV on `Instrumento` (already existed). Caller CSV = `rol,tipoEmpleado`. Upgrade script honors template top-level `rolesPermitidos`.
**MATRIX_TIPOS**: `GERONTOLOGA \| CONTRATOS \| PROFESORES \| AUXILIARES`.

Tests: `backend/tests/rbac/aug6-features.spec.ts` (25), `matrix-parity.spec.ts` (4 profiles × domains), `frontend/tests/rbac/roles-aug-6.spec.ts` (8), `crear-template.spec.ts` (4, incl. codigo-required).

### 3.4 qa-session-aug-6 followup (this session, committed in `8e07055` + staged extras)
Source: `context/user-feedback/qa-session-aug-6.md`

| # | Item | Key impl |
|---|---|---|
| CONTRATOS edit contratos | Always "Insufficient permissions" | **Root cause**: `requireRole('ADMIN')` on POST/PUT/DELETE `/nomina/employees/:id/contratos*`. Fix: `requireEmployeeUnlocked('id')` only (`requireDomain('nomina')` already allows CONTRATOS). Locked still 403 `EMPLOYEE_LOCKED`. |
| Drop patient fields | Signos + Boletín duplicated cédula/nombre | **v2** templates: SIGNOS = only `mediciones`; BOLETIN = `periodo` (`boletin_periodo_anio`) + `componentes` + `autor`. Patient identity from paciente entity. |

Staging Playwright: `backend/tests/staging/qa-aug6-followup-staging.spec.ts` **6/6** on live API.

---

## 4) Authoritative contracts (SSOT — do not invent names)

| Topic | Path |
|---|---|
| qa-jul-31 (Nequi label, nomina valorMensual, EPS/ARL) | `development/qa-session-jul-31/orchestration-ctx/decisions/contract-schema-qa-jul-31.md` |
| aug-04 lock + TINETTI | `development/qa-session-jul-31/followup-aug-04-completion.md` |
| aug-6 roles/instruments/certificados | `development/fixes-features-aug-6/orchestration-ctx/decisions/contract-fixes-features-aug-6.md` |
| qa-jul-24 (payment/cargos/asistencia/salary) | `development/qa-session-jul-24/orchestration-ctx/decisions/contract-schema-qa-jul-24.md` |
| RBAC base (TipoEmpleado) | `development/fixes-jul17-2/orchestration-ctx/decisions/contract-fixes-jul17-2.md` |
| Instruments base | `development/instrumentos-dynamic-fichas/orchestration-ctx/decisions/schema-contract-instrumentos-dynamic-fichas.md` |
| Nómina/asistencia | `development/nomina-asistencia-jul-18/orchestration-ctx/decisions/schema-contract-nomina-asistencia-jul-18.md` |

**Live matrix file (code)**: `backend/src/middleware/domainAccess.ts` + FE mirror `frontend/app/composables/useDomainAccess.ts`. QA cell-by-cell: `backend/tests/rbac/matrix-parity.spec.ts`.

---

## 5) Key code patterns (where success lived)

| Pattern | Where | How to extend |
|---|---|---|
| Domain RBAC | `domainAccess.ts` `DOMAIN_ACCESS` + `requireDomain` | Add cell; mirror FE; extend `MATRIX_TIPOS`; update matrix-parity |
| Employee lock | `requireEmployeeUnlocked(param)` | Apply on non-admin mutating routes that take empleado id |
| Payment/EPS fields | Zod in `employees.routes.ts`; `validate()` writes **stripped** parse to `req.body` | Keep lock fields **out** of update schema |
| Contract salary | `nominaService.resolveCalcFields`; FE `usaValorMensual` | OPS→valorJornada; others→valorMensual |
| Instruments | `backend/prisma/instrument-templates/{CODIGO}.v{n}.json` + `npm run instruments:upgrade` | Highest version auto-activated. Staging: tunnel + `FORCE_UPGRADE=true` (script refuses deployed DBs). **tsx is NOT on the instance** — run upgrade from laptop via SSH tunnel `:5433`. |
| Definition fetch | FE `GET /instruments/:codigo/definition` | **codigo required** on create-from-template or detail shows "no llenable" |
| Scoring result | `instrumentScoringService.computeScore` | Field is **`puntajeTotal`**, not `total` |
| Auth cookie | Live `backend/src/routes/auth.ts` cookie name **`session`** | Do not revive `auth.routes.ts` / `sessionToken` |
| Lightsail SSH | `~/.ssh/miempresa-lightsail-key.pem` | Not Lightsail temp-cert; not SSM |
| Local BE restart | `lsof -ti :3101` then `PORT=3101 npm run dev` | `tsx watch` can serve stale compiled routes |

**v-model pitfall** (memory): `v-model` on `const reactive()` drops child emits — use `:model-value` + handlers. Tests must fill DatePickers + optional fields.

---

## 6) Migrations (local = staging = 28)

| # | Dir | What |
|---|---|---|
| 24 | `20260731203612_qa_jul24_cargos_efectivo_valormensual` | EFECTIVO, valor_mensual, cargos recreate |
| 25 | `20260803022244_add_empleado_eps_fondo_arl` | eps, fondo_pensiones, arl |
| 26 | `20260804165919_add_empleado_bloqueado` | bloqueado, bloqueado_por, bloqueado_en |
| 27 | `20260805000000_centro_costos_ago5` | **drops empty** egresos/prefacturas/productos_servicios; `centro_costos_items` |
| 28 | `20260806035159_add_tipoempleado_profesores_auxiliares` | enum ADD VALUE PROFESORES, AUXILIARES |

Gitignored — only travel via CodeDeploy zip.

---

## 7) Verification commands

```bash
# Jul-31 / lock / contratos role
cd backend && TEST_API_URL=http://localhost:3101 npx playwright test \
  tests/employees/empleado-eps-fondo-arl.spec.ts \
  tests/employees/empleado-bloqueo.spec.ts \
  tests/nomina/nomina-sugerencia-valormensual.spec.ts \
  tests/nomina/contratos-contratos-role-edit.spec.ts \
  tests/instruments-dynamic/signos-boletin-v2-no-paciente.spec.ts \
  tests/rbac/aug6-features.spec.ts \
  tests/rbac/matrix-parity.spec.ts --reporter=list

# Staging live (needs SSM passwords)
TEST_API_URL=https://miempresa-api-stg.disruptiveexp.com/api/v1 \
  QA_ADMIN_PASSWORD=… QA_CONTRATOS_PASSWORD=… \
  npx playwright test tests/staging/qa-aug6-followup-staging.spec.ts --reporter=list

# FE mocked
cd frontend && TEST_FRONTEND_URL=http://localhost:3100 npx playwright test \
  tests/local-qa/jul31-qa-frontend.spec.ts \
  tests/local-qa/jul24-qa-frontend-contract-monthly.spec.ts \
  tests/rbac/roles-aug-6.spec.ts \
  tests/instruments-dynamic/crear-template.spec.ts --reporter=list
```

Pre-existing TEST-ENV (do not "fix" by loosening): patient-notes TZ (UTC vs Bogotá), some instruments-dynamic Tailscale-IP + TINETTI item-count drift.

---

## 8) Staging release play (next time)

Pattern: `.claude/skills/planify-team/release-protocol.md` + latest runbook.
1. R0 preflight: health, migrate counts, **row counts on any table a migration touches**, list ride-alongs in dirty tree.
2. R1 on-instance `pg_dump \| gzip` → `s3://miempresa-backups-…-staging/pre-releases/`. Verify local=S3 size.
3. R4: `npm run build` in `backend/`; rsync exclude node_modules/tests; **copy `infrastructure/db/appspec.yml` to zip root**; upload; `create-deployment` group **`miempresa-staging` only**. AfterInstall runs `prisma migrate deploy`.
4. Instruments: from laptop, SSH tunnel `5433:localhost:5432`, `FORCE_UPGRADE=true DATABASE_URL=… npm run instruments:upgrade`.
5. R5: `./frontend/infrastructure/scripts/deploy-frontend.sh --stage staging --region us-east-1 --profile disruptive`.
6. Latest examples: `staging-release-qa-jul31-aug04-runbook.md`, `staging-release-fixes-features-aug-6-runbook.md`, `staging-release-qa-aug6-followup-runbook.md`.

---

## 9) Open / next

1. **Huge uncommitted tree** (~119 files): lock UI/service, EPS fields, nomina valorMensual dialog, PROFESORES/AUXILIARES RBAC + tests, domainAccess, schema, centro-costos feature, most context/docs. HEAD `8e07055` only has a slice (templates + nomina guard + crear.vue + some tests).
2. Seed QA users for PROFESORES / AUXILIARES if those roles will be used in staging QA.
3. Centro-costos shipped to staging (ride-along on aug-6) but was **not independently QA'd** in the jul-31 session.
4. Pre-existing debt: certificates list filter 500 (jul-22); print CSS; patient-notes TZ tests.

---

## 10) Orchestration lessons (carry forward)

- Contract-first still prevents name mismatches; **verify hypotheses by repro** (gerontologa bug was FE, not rolesPermitidos intersection).
- `instruments:upgrade` + `FORCE_UPGRADE=true` is the only safe way to publish template versions on staging.
- Working-tree deploy = implicit multi-feature release — call it out at R0.
- Lightsail SSH: established PEM, not improvised temp-cert.
- Do not trust `tsx watch` after route-guard edits — restart PID on :3101.
- Playwright: es-CO number format `3,000,000`; heading locators need `level: 1` when h1+h2 share text; fill `contrato-fecha-fin` for non-INDEFINIDO.
- Workers: PARK after COMPLETE; one INTERVENTION on unexpected idle; never blanket-pkill.

---

## 11) Grep hooks

```
resume-session aug-17 8e07055 28-migrations
d-13RWC0Q4L amplify-job-15  d-MR1QVXIZK amplify-job-14  d-EDEN0GJYK amplify-job-13  d-OJON0UVVK amplify-job-12
Nequi/Bre-B usaValorMensual eps fondoPensiones arl bloqueado requireEmployeeUnlocked
PROFESORES AUXILIARES read-only rolesPermitidos SIGNOS_VITALES BOLETIN_ANUAL TINETTI.v5
puntajeTotal FORCE_UPGRADE miempresa-lightsail-key.pem sessionToken-dead
contrato-fecha-fin EMPLOYEE_LOCKED qa-aug6-followup
```

---

## 12) Doc index

| Kind | Path |
|---|---|
| **This resume** | `context/resume-session/summary-2026-08-17.md` |
| Prior resume | `context/resume-session/summary-2026-07-31.md` |
| jul-31 cleaned / implemented | `context/user-feedback/qa-session-jul-31-cleaned.md` · `context/plan-implemented/qa-session-jul-31-implemented.md` |
| jul-31 contract / handoff | `development/qa-session-jul-31/orchestration-ctx/decisions/contract-schema-qa-jul-31.md` · `06-handoff.md` |
| aug-04 followup | `development/qa-session-jul-31/followup-aug-04-completion.md` |
| aug-6 contract / handoff | `development/fixes-features-aug-6/orchestration-ctx/decisions/contract-fixes-features-aug-6.md` · `06-handoff.md` |
| aug-6 QA raw | `context/user-feedback/qa-session-aug-6.md` |
| Runbooks | `context/implementation-plan/staging-release-qa-jul31-aug04-runbook.md` · `staging-release-fixes-features-aug-6-runbook.md` · `staging-release-qa-aug6-followup-runbook.md` |
| Release protocol | `.claude/skills/planify-team/release-protocol.md` |
