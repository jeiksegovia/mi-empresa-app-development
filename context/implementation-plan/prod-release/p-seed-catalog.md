# P-SEED-CATALOG — WAITING

```bash
cd backend/prisma/prod-db
./sync-prod-catalog.sh --profile disruptive --region us-east-1 --empresa --cargos --centros
```

Phrase: `sync prod catalog`. EMKASA, 15 cargos, 14 centros, prices 1.5M / 70k.
