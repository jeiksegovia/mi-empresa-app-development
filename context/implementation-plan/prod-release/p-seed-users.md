# P-SEED-USERS — WAITING (run once)

```bash
cd backend/prisma/prod-db
./seed-prod.sh --profile disruptive --region us-east-1
```

Phrase: `seed prod foundation`. Refuses if `usuarios` has any row. Then `FORCE_UPGRADE=true` instruments (includes VALORACION v2).
