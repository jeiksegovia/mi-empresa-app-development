{
  "AppFunction": {
    "NODE_ENV": "development",
    "STAGE": "local",
    "DATABASE_URL": "postgresql://miempresa:miempresa123@host.docker.internal:15432/miempresa_dev",
    "JWT_SECRET": "dev-secret-change-me",
    "JWT_EXPIRATION": "24h",
    "CORS_ORIGIN": "http://localhost:3000,http://localhost:3001",
    "LOG_LEVEL": "debug"
  },
  "MigrationFunction": {
    "DATABASE_URL": "postgresql://miempresa:miempresa123@host.docker.internal:15432/miempresa_dev"
  }
}
